spring:
  application:
    name: API-GATEWAY

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true      # dynamic routes from Eureka

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8080/eureka/
